import {
    Sidenav,
    Dropdown,
    Input,
    Ripple,
    initTE,
} from "tw-elements";

initTE({ Sidenav, Dropdown, Input, Ripple });

export default {
    Sidenav,
    Dropdown,
    Input,
    Ripple
};
