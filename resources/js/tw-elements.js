import {
    Sidenav,
    Dropdown,
    Input,
    Ripple,
    Alert,
    initTE,
} from "tw-elements";

initTE({ Sidenav, Dropdown, Input, Alert, Ripple });

export default {
    Sidenav,
    Dropdown,
    Input,
    Alert,
    Ripple
};
